import math 					# Módulo --> importar uma biblioteca de funções (sem esse import as funções abaixo não funcionam)

print(math.sqrt(9.0))			# square root
print(math.pow(2.0, 3.0))		# potência (2^3)
print(math.exp(5))				# exponencial (e^x)
